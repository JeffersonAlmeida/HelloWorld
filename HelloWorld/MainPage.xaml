<phone:PhoneApplicationPage 
    x:Class="HelloWorld.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:HelloWorld.View"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivot Control-->
        <controls:Pivot x:Name="PivotPlatform" Margin="0,0,8,77" Title="Nubank" ItemsSource="{Binding PivotItems}" >
            
            <controls:Pivot.HeaderTemplate>
                <DataTemplate>
                    <local:PivotTemplateSelector Content="{Binding}">
                       
                        <local:PivotTemplateSelector.Overdue>
                            <DataTemplate>
                                <TextBlock Text="{Binding State}" FontSize="40" Foreground="Green"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Overdue>

                        <local:PivotTemplateSelector.Open>
                            <DataTemplate>
                                <TextBlock Text="{Binding State}" FontSize="40" Foreground="Red"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Open>

                        <local:PivotTemplateSelector.Closed>
                            <DataTemplate>
                                <TextBlock Text="{Binding State}" FontSize="40" Foreground="Blue"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Closed>

                        <local:PivotTemplateSelector.Future>
                            <DataTemplate>
                                <TextBlock Text="{Binding State}" FontSize="40" Foreground="Orange"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Future>

                    </local:PivotTemplateSelector>
                </DataTemplate>
            </controls:Pivot.HeaderTemplate>

            <controls:Pivot.ItemTemplate>
                <DataTemplate>
                    <local:PivotTemplateSelector Content="{Binding}">
                       
                        <local:PivotTemplateSelector.Overdue>
                            <DataTemplate>
                                <TextBlock Text="{Binding Id}" FontSize="40" Foreground="Green"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Overdue>

                        <local:PivotTemplateSelector.Open>
                            <DataTemplate>
                                <TextBlock Text="{Binding Id}" FontSize="40" Foreground="Red"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Open>

                        <local:PivotTemplateSelector.Closed>
                            <DataTemplate>
                                <TextBlock Text="{Binding Id}" FontSize="40" Foreground="Blue"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Closed>

                        <local:PivotTemplateSelector.Future>
                            <DataTemplate>
                                <TextBlock Text="{Binding Id}" FontSize="40" Foreground="Orange"/>
                            </DataTemplate>
                        </local:PivotTemplateSelector.Future>


                    </local:PivotTemplateSelector>
                </DataTemplate>
            </controls:Pivot.ItemTemplate>
            
        </controls:Pivot>
    </Grid>
 
    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>